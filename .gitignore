# Dependências
/vendor/

# Ambiente local (contém senhas)
.env

# Dados e logs gerados em runtime
/data/*.sqlite
!/data/hidro.sqlite
/logs/*.log

# Backups gerados automaticamente (o backup inicial está em database/ e é versionado)
/database/hidro_backup_*.sql
/data/backup_hidro_*.json

# Sistema operacional
.DS_Store
Thumbs.db

# Modelo gerado pelo treino — não versionar automaticamente
data/mlr_coefs.json
